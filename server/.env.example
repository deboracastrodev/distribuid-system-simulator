# Kafka Configuration
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
KAFKA_TOPIC=orders
KAFKA_DLQ_TOPIC=orders-dlq
KAFKA_CONSUMER_GROUP=nexus-server-group

# Postgres Configuration
POSTGRES_DSN=postgres://nexus_user:nexus_pass@localhost:5432/nexus_db?sslmode=disable

# HTTP (/health, /metrics)
SERVICE_PORT=8080

# OpenTelemetry Configuration
OTEL_EXPORTER_OTLP_ENDPOINT=localhost:4317
OTEL_SERVICE_NAME=nexus-server

# Webhook Configuration
WEBHOOK_URL=http://localhost:9090/webhook
WEBHOOK_WORKERS=4
WEBHOOK_MAX_ATTEMPTS=10

# Circuit breaker do webhook (duracoes no formato Go: 30s, 1m)
WEBHOOK_CB_FAILURE_THRESHOLD=5
WEBHOOK_CB_SUCCESS_THRESHOLD=2
WEBHOOK_CB_OPEN_DURATION=30s
WEBHOOK_TIMEOUT=10s
